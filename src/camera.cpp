#include "camera.h"
#include <cmath>

// TODO: compute camera basis and image plane
Camera::Camera(double vfov, double aspect) {
    // stub
}

// TODO: map (u,v) in [0,1] to ray direction
Ray Camera::generateRay(double u, double v) const {
    return Ray();
}
